# v2026.09.25

Portable Windows build.

Included:

- `RulateAutoLiker.exe`
- bundled Playwright Chromium in `ms-playwright`
- README and config example

Changes:

- Updated bookmarks for Rulate's current POST form and bookmark-type menu.
- Confirmed bookmark, like, and rating state before recording success.
- HTTP 429 and other blocking responses stop the current pass without marking actions complete.
- Previously skipped bookmarks are returned to the queue on first run.
- Updated bundled Chromium to match Playwright in the EXE.

Not included:

- user `data` folder, Telegram sessions, browser profile or cookies
- local `config.json` and SQLite processing database
