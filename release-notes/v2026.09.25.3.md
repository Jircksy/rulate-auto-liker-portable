# v2026.09.25.3

The backlog cap now runs immediately after the scan-only command as well as before each automatic processing pass. Repeated scans cannot accumulate more than the configured number of pending books.

Verified with 120 initial Telegram links and a second scan that added 80 more: only the newest 50 pending books remained in the queue. Explicit manual force runs remain unrestricted.

The bundled Chromium revision is unchanged from v2026.09.25. Users of that release or later can update by replacing the EXE. Users of v2026.06.15 should install the full portable ZIP while preserving `config.json` and `data`.

Assets: full portable ZIP and standalone EXE.
